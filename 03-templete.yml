- name: Demo on copying the file to the remote machines with a template 
  hosts: all 
  tasks:
    - name: copy a proxy file to remote nodes 
      ansible.builtin.template:
        src: proxyfile.config
        dest: /home/centos 